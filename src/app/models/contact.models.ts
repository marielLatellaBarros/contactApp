export class Contact {
    name: string;
    email: string;
    phone: string;
    isFavourite: boolean;
    avatar: string;

    constructor (
        name: string,
        email: string,
        phone: string,
        isFavourite: boolean,
        avatar: 'assets/avatar.png',) {
    }
}